Задачи отмечены выполненными: изменение оформлено задним числом, код написан
и проверен до появления этих артефактов.

## 1. Настройка

- [x] 1.1 Добавить в конфигурацию пропуск на следующее звено: флаг
  `--upstream-key`, переменная `CLAUDE_PROXY_UPSTREAM_KEY`, обрезка пробелов
- [x] 1.2 Запретить старт с пропуском при апстриме на Anthropic; сравнение
  имени хоста без учёта регистра и завершающей точки
- [x] 1.3 Экспортировать перечень переменных, которыми распоряжается шлюз
  (`ManagedEnvKeys`), чтобы установщик отличал их от дописанных оператором

## 2. Проксирование

- [x] 2.1 Удалять клиентский `X-Gateway-Key` перед обращением к апстриму в
  обоих режимах и подставлять вместо него пропуск на следующее звено
- [x] 2.2 Признать `X-Api-Key` credential'ом в режиме `oauth` — запрос от
  предыдущего звена в режиме `apikey`
- [x] 2.3 Пробросить настройку из конфигурации в шлюз

## 3. Установка сервиса

- [x] 3.1 Писать пропуск на следующее звено в `EnvironmentFile`, пустое
  значение не писать
- [x] 3.2 Переносить в новый `EnvironmentFile` строки оператора, не перенося
  legacy-имена; сообщать о переносе в выводе установки

## 4. Тесты

- [x] 4.1 Тесты подстановки и зачистки заголовков в обоих режимах
- [x] 4.2 Тест признания `X-Api-Key` credential'ом в режиме `oauth`
- [x] 4.3 Тесты цепочки из двух звеньев за TLS: `oauth` → `oauth` и
  `apikey` → `oauth`
- [x] 4.4 Тесты валидации пропуска и чтения его из переменной окружения
- [x] 4.5 Тесты установщика: запись пропуска и перенос строк оператора

## 5. Проверка схемы на стенде

- [x] 5.1 Скрипт `scripts/chain-smoke.sh`: заглушка вместо Anthropic, два
  звена, обе рабочие комбинации режимов и отказ при неверном пропуске
- [x] 5.2 Цель `make smoke`; на macOS скрипт сообщает SKIP
- [x] 5.3 Включить проверку в `ci` и `release` перед сборкой

## 6. Документация

- [x] 6.1 Раздел «Цепочка шлюзов» в `docs/advanced.md`: настройка, доверие
  между звеньями, комбинации режимов, цена, проверка на стенде
- [x] 6.2 Строка настройки в таблице конфигурации и упоминание в диагностике
- [x] 6.3 Отметить в `README.md` цепочку среди сценариев из advanced
- [x] 6.4 Дополнить «Ключевые места реализации» зачисткой заголовка и
  признанием `X-Api-Key`

## 7. Приёмка

- [x] 7.1 `make check` зелёный (gofmt, vet, тесты)
- [ ] 7.2 Ветка `feature/add-gateway-chaining`, PR с зелёным CI — включая
  первый реальный прогон `make smoke` на Linux
- [ ] 7.3 После мержа — `openspec archive add-gateway-chaining`
