Ветка `feature/plain-listener` от актуального `main`; один PR. Архивация —
процессный шаг после мержа, в задачах не числится.

## 1. Конфигурация

- [x] 1.1 Добавить `config.TLSNone = "none"` в `TLSSource`, принять его в
  `Resolve()`; обновить текст ошибки «ожидается auto, files, self или none»
- [x] 1.2 В `validate()` для `none`: хост из `--listen` обязан быть
  loopback (`localhost` без учёта регистра или IP с `IsLoopback()`),
  пустой хост — отказ; текст ошибки называет режим и объясняет, что
  открытым текстом ушли бы пропуски и токены подписки
- [x] 1.3 Убедиться, что для `none` не требуются `--domain`, `--state-dir`,
  `--cert-file`, `--key-file`, а проверка `https` для апстрима действует
  без изменений
- [x] 1.4 Обновить текст справки флага `--tls` в `main.go`

## 2. Слушатель

- [x] 2.1 `tlsconf.New()`: для `TLSNone` вернуть `Provider` без `TLSConfig`
  с `Description` «без TLS (только loopback)»
- [x] 2.2 `server.Run()`: при `provider.TLSConfig == nil` не выставлять
  `TLSConfig` и вызывать `Serve(ln)` вместо `ServeTLS`; ACME-ветка не
  поднимается

## 3. Клиентские переменные и установка

- [x] 3.1 `clientenv.baseURL()`: для `none` — `http://` + хост из `--listen`
  + порт; для остальных источников поведение прежнее
- [x] 3.2 `service.waitHealthy()`: для `none` проверять
  `http://127.0.0.1:<port>/healthz` без TLS-транспорта и SNI; прокинуть
  признак из `Resolved.TLS`

## 4. Тесты

- [x] 4.1 Табличный тест валидации: принимаются `127.0.0.1:9443`,
  `[::1]:9443`, `localhost:9443`, `LOCALHOST:9443`; отвергаются `:9443`,
  `0.0.0.0:9443`, `10.0.0.5:9443`, `[::]:9443`
- [x] 4.2 Тест: `none` стартует без `--domain` и `--state-dir`; `none` с
  апстримом `http://` отвергается
- [x] 4.3 Тест сервера: запуск с `none` на `127.0.0.1:0`, запрос по `http`
  с верным пропуском доходит до заглушки апстрима, без пропуска — `401`
- [x] 4.4 Тест цепочки: plain-звено на loopback → TLS-звено → заглушка
  Anthropic; внешнее звено видит `X-Gateway-Key` со своим пропуском
- [x] 4.5 Тест `clientenv`: для `none` печатается
  `ANTHROPIC_BASE_URL=http://127.0.0.1:9443`
- [x] 4.6 Тест `waitHealthy` по `http` (по образцу `health_test.go`)

## 5. Проверка на стенде

- [x] 5.1 Вручную: локальное `none`-звено на ноутбуке → реальный внешний
  шлюз; Claude Code с `ANTHROPIC_BASE_URL=http://127.0.0.1:9443` проходит
  запрос. Если CLI не принимает `http://` — остановиться и обсудить,
  документация не пишется

## 6. Документация

- [x] 6.1 `docs/advanced.md`: строка `--tls` в таблице конфигурации;
  подраздел `none` в «Источники сертификата» — когда допустим, почему
  только loopback, что не требуется, `client-env`
- [x] 6.2 `docs/advanced.md`, «Цепочка шлюзов»: пример локального звена на
  ноутбуке — команда `run` с `--tls none`, блок клиентских переменных без
  `NODE_EXTRA_CA_CERTS`, оговорка про доверие к сертификату внешнего звена
  на macOS
- [x] 6.3 `README.md`: одна фраза со ссылкой на сценарий «локальное звено
  без TLS» в списке других сценариев

## 7. Финал

- [x] 7.1 `make check` зелёный (gofmt, vet, тесты)
- [x] 7.2 PR в `main`, сообщение коммита на русском
