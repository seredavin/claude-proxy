Ветка `fix/ipv4-mapped-ipv6` от актуального `main`; один PR, архив в той
же ветке.

## 1. Детектор

- [x] 1.1 `ipv6Candidate` по D1; для `::ffff:a.b.c.d` совпадение сужается
  до IPv4-хвоста (D2), прочие IPv6 с хвостом — целиком
- [x] 1.2 `maskable`: `Unmap()` перед проверками loopback, неопределённого
  и `ip private` (D4)

## 2. Суррогат

- [x] 2.1 `surrogateFor` → обёртка с блокировкой + `surrogateLocked`;
  для `Is4In6()` — суррогат вложенного IPv4, формат `::ffff:%04x:%04x`
  (D3), с ключом и без

## 3. Тесты и документация

- [x] 3.1 Тесты по сценариям дельты: `[::ffff:10.0.0.1]:22` согласован с
  `10.0.0.1` и без фрагментов исходного адреса; hex-mapped — суррогат в
  `192.168.0.0/16` и обратная подстановка; `ip private` с mapped loopback,
  публичным и приватным; NAT64 и `::10.0.0.1` — без точечного хвоста и
  октетов исходного; всё то же с ключом
- [x] 3.2 `docs/advanced.md`: в таблице правил — IPv4-mapped адреса
- [x] 3.3 `make check`, `openspec validate --strict`, архив в ветке, PR
