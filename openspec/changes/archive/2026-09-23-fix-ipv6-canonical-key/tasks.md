Ветка `fix/ipv6-canonical-key` от актуального `main`; один PR, архив в той
же ветке.

## 1. Ключ таблицы

- [x] 1.1 `tableKey(value, category)` по D1; `surrogateLocked` и поиск в
  `maskText` (`create=false`) используют её

## 2. Тесты

- [x] 2.1 Без ключа и с ключом: `FD00::1`, `fd00::1`, `fd00:0::1`,
  `fd00:0000:0000:0000:0000:0000:0000:0001` — один суррогат, одна запись
  таблицы; обратная подстановка — в записи первого появления; публичный
  IPv6 в двух записях — один суррогат; hex-mapped в двух регистрах
  сохраняет свою запись
- [x] 2.2 Ход модели (`create=false`) с другой записью известного адреса
  перемаскируется тем же суррогатом, включая hex-mapped в другом регистре
  (D1a)

## 3. Завершение

- [x] 3.1 `make check`, `openspec validate --strict`, кросс-ревью, архив в
  ветке, PR
