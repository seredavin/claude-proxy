# Реальные секреты — никогда не коммитить
.env
.env.bak
.env.local

# Сертификаты, если вдруг положили рядом с проектом
*.pem
*.key
*.crt
certs/

# Артефакты сборки
/claude-proxy
/dist/

# Локальные разрешения Claude Code — у каждого свои
.claude/settings.local.json
